import * as auth from "./entities/auth";
import * as users from "./entities/users";
import * as settings from "./entities/settings";
import * as files from "./entities/files";
import * as collections from "./entities/collections";

export const routes = {
  auth,
  users,
  settings,
  files,
  collections,
};
